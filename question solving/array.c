#include<stdio.h>
int main(){
    int n;
    int arr[m],max=arr[0];
    printf("enter the number of elements:");
    scanf("%d",&m);

    printf("elements %d:%d",i+1,)

    
    
    for(int i=1;i<5;i++){
        if(max<arr[i]){
            max=arr[i];
        }
    }
    printf("%d",max);

    return 0;
}